name: dongphuc296web
description: Dong phuc 296 web
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.0-0 <3.0.0"

dependencies:
  adaptive_navigation: ^0.0.3
  collection: ^1.15.0
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  path_to_regexp: ^0.4.0
  quiver: ^3.1.0
  url_launcher: ^6.1.1
  url_strategy: ^0.2.0
  window_size:
    git:
      url: https://github.com/google/flutter-desktop-embedding.git
      path: plugins/window_size
  firebase_core :

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  test: ^1.16.0

flutter:
  uses-material-design: true

  assets:
    - packages/flutter_gallery_assets/
    - packages/flutter_gallery_assets/products/
    - packages/flutter_gallery_assets/products/clothing/
    - packages/flutter_gallery_assets/products/canvas/

